/* if we can't get cron jobs to work, just call endpoint that triggers email sending in front-end */
// import React, { useEffect, useState } from 'react';

// export default function Notifications() {
// const fetchNotifications = async () => {
//     //try {const response = await fetch(`/api/loads`)} // don't rlly care about response, just need to hit this endpoint
//     catch (error) {
//         // eslint-disable-next-line no-console
//         console.error("Error fetching notifications:", error);
//     }
// }
// fetchNotifications();
//   return;
// }
